manifestVersion: subsquid.io/v0.1
name: gs-main-karura
version: 1
description: |-
  Giant Squid Explorer for Karura chain
build:
deploy:
  addons:
    postgres:
  processor:
    env:
      CHAIN: karura
      CHAIN_RPC_ENDPOINT: ${{ secrets.RPC_KARURA_HTTP }}
    cmd:
      - sqd
      - process:prod
  api:
    cmd:
      - sqd
      - serve:prod
scale:
  dedicated: true
  addons:
    postgres:
      storage: 30G
      profile: medium
  api:
    profile: medium
  processor:
    profile: medium
