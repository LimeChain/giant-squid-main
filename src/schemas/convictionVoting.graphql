type Delegate @entity {
  id: ID!
  class: Int
  to: String
  conviction: String
  balance: String
}

type Unlock @entity {
  id: ID!
  class: Int
  target: String
}

type Undelegate @entity {
  class: Int
}
